/*
package Rearanging;

public class BubbleSort {
    private static int x;

    public static void main(String[] args) {
        int[] numberArray = {4, 5, 6, 2, 1, 7};

    }

    static int[] sort(int[] array) {
        int n = array.length;
        for (int i = 0; i < n - 1; i++) {
            if (array[i] > array[i + 1]) {
                int tmp = array[i];
                array[i] = array[i + 1];
                array[i] = tmp;

            }
        }
        n = n - 1;
        while (n > 1) {
            break;
        }

        return array[];
    }
}
*/
